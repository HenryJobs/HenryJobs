export interface allUserInterface {
    _id: string,
    firstName: string,
    lastName: string,
    email: string,
    password: string
};

export interface userByNameInterface {
    firstName?: any,
    lastName?: any,
};